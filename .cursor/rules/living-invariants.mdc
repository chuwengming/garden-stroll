---
description: >-
  Living project invariants — maintain docs/invariants.md as an executable
  global contract while building and optimizing any project. Apply when
  starting or developing a project, adopting stage-verifier advice, or changing
  cross-step behavior.
alwaysApply: true
---

# Living Invariants（可執行全局契約）

適用於**任何專案**。開發／調整／採納優化時必須遵守。契約是活文件，隨行為演進累積，不是結案才補。

## 1. 檔案位置與建立時機

- 契約路徑固定為：`docs/invariants.md`（若專案慣用其他 docs 根目錄，仍優先此路徑；僅在已有明確慣例時可改，並在檔內註明）。
- **若不存在**：在第一個「可運行里程碑」（能啟動／能走通一條主路徑）達成時**立即建立**，不可拖延到專案「完整」才寫。
- **若已存在**：動手改碼前先讀；行為與契約不符時，**先更新契約再繼續**（或同一次變更內同步更新）。

## 2. 建立時必須使用此骨架

新建 `docs/invariants.md` 時，至少包含下列章節（可依專案刪減空章，但不得省略「流程／環境／禁止破壞／待確認」）：

```markdown
# Project Invariants（可執行全局契約）

> 隨專案演進持續累積。每條應可被人工或 agent 驗證（可檢查、可回歸）。
> 最後更新：YYYY-MM-DD

## 1. 產品流程
- [ ] …

## 2. 模式／分支（若有多模式、多角色、多入口）
- [ ] …

## 3. 環境與銜接（mock / simulation / real）
- [ ] …

## 4. 資料與設定（目錄、env 語意、預設值）
- [ ] …

## 5. UI／跨頁／跨模組契約（共用 store、handoff、顯示尺寸等）
- [ ] …

## 6. 禁止破壞（含已修回歸）
- [ ] …（可連到 `docs/agent-memory/` 或同等錯誤記憶）

## 7. 待確認
- [ ] …
```

條目寫法要求：

- **可驗證**：寫「Dot 模式 Process→Next 必須進 Analyse、不得進 Editor」，不要寫「流程要正確」。
- **不確定**：寫入「## 7. 待確認」，寧可標疑點也不要省略。
- **已修過的坑**：記入「## 6. 禁止破壞」，並盡量連到錯誤記憶檔。

## 3. 何時必須更新契約

下列任一發生時，必須更新 `docs/invariants.md`（同一次工作內完成）：

- 新增或改變使用者可見主流程／步驟順序
- 新增模式分支（例如 Gel vs Dot、角色權限、A/B 路徑）
- 改變 mock／simulation／real 銜接、env 語意、預設模式
- 改變跨頁 handoff、共用 store、共用 viewport／影像顯示契約
- 資料目錄或「權威資料來源」變更
- 修復回歸 bug（同步寫入禁止破壞）
- 刻意廢棄舊路徑／舊目錄（寫明舊→新，避免之後又「優化回去」）

沒有契約變更時，回合結束可簡短註明「無契約變更」。

## 4. 採納優化／stage-verifier 建議（強制過濾）

對 stage-verifier 或任何「優化／重構」建議：

1. **先對照** `docs/invariants.md`（及既有 `docs/agent-memory/` 若存在）。
2. 每條建議標記其一：`可採` / `需改寫後可採` / `不適用（與契約衝突）` / `需確認`。
3. 與契約或已知回歸衝突者：**不得當必改**；不可為了「程式乾淨」而統一 API、刪 mock 分支、合併雙軌 base URL，除非已證明不破壞契約。
4. 僅對使用者批准（或明確要求落地）的 `可採`／改寫後清單動手。

## 5. 改碼紀律（配合契約，避免掛一漏萬）

局部修改前，簡要自答（可寫在思考或回覆中）：

1. 影響哪些使用者路徑？（含各模式、mock/real）
2. 哪些相鄰步驟共用 store／CSS／session／API？
3. 是否觸及「禁止破壞」或待確認條目？
4. 最小驗證是什麼？（2～3 項即可）

規則：

- 禁止「順便重構」無關模組。
- 必須改契約時：先寫明**舊 → 新**，再改碼。
- 改完後，行為必須仍符合更新後的 invariants；否則先修契約或回滾改動。

## 6. 回合結束回報（有契約變更時）

若本輪更新了契約，用極短清單回報：

- 新增／修改／刪除了哪些 invariants 條目（標題級即可）

## 7. 與其他機制的關係

- **stage-verifier**：可繼續產出建議，但落地前必須經本 rule 的契約過濾。
- **錯誤記憶**（如 `docs/agent-memory/loop-failures.md`）：與「禁止破壞」互補；新回歸修復兩邊都記。
- 本 rule **不取代**專案專屬技術規範；專案另有 `.cursor/rules` 時一併遵守，衝突時以**使用者當下指示**與**更新後的 invariants**為準。

